# Observation
Source: `obs_openmrs`. `id` = `uuid`; `subject` = `Patient/<person.uuid>`; optional
`encounter`. `code` from `concept_name` (CIEL codings pending `concept_reference_*`).
`value[x]` from the populated `value_*` column (numeric/coded/datetime/text). `voided=0`.
