# Encounter
Source: `encounter_openmrs`. `id` = `uuid`; `subject` = `Patient/<person.uuid>`;
`period.start` = `encounter_datetime`. `class` defaults to AMB (refine via `encounter_type`
once the dimension rows are provided). `voided=0`.
